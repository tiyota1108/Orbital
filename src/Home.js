import React from 'react';
import { Link } from 'react-router-dom';
import './home.css';

const Home = (props) => (
  <div id = "login">
    <Link to = "/login"> Login/Register </Link>
  </div>

)
export default Home;
